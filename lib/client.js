'use strict';

const Bitcore = require('bitcore-wallet-client');

/**
 * Instantiate a Bitcore Wallet Client
 */
class Client {
  constructor(options) {
    this.options = options.
  }
}

module.exports = Client;